#!/bin/sh
# Fast local checkpoint: architecture rules + typecheck. Full test suite
# runs at pre-push; the platform matrix is CI's job (see AGENTS.md).
set -e

npx depcruise src bin test --config .dependency-cruiser.cjs
npm run typecheck
